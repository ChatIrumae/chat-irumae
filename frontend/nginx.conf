server {
  listen 80;
  server_name localhost;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # React Router 등을 사용할 때 새로고침 시 404 에러 방지
    try_files $uri $uri/ /index.html;
  }
}